import React,{useEffect,useState} from 'react'; import {api} from '../services/api';
export default function Financial(){const [d,setD]=useState(null);useEffect(()=>{api.get('/financial/dashboard').then(r=>setD(r.data))},[]);return <div className="space-y-6"><h1 className="text-3xl font-bold">Financeiro</h1><div className="grid sm:grid-cols-3 gap-4">{[['Faturamento',d?.totalRevenue],['Custos',d?.totalRevenue-(d?.totalProfit||0)],['Lucro',d?.totalProfit]].map(([n,v])=><div className="bg-white border rounded-2xl p-5" key={n}><div className="text-gray-500">{n}</div><b className="text-2xl">{Number(v||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'})}</b></div>)}</div></div>}