import React,{useState} from 'react'; import {useNavigate} from 'react-router-dom'; import {useAuth} from '../contexts/AuthContext';
export default function Login(){const {login,register}=useAuth();const go=useNavigate();const [mode,setMode]=useState('login');const [form,setForm]=useState({name:'',email:'',password:''});const [error,setError]=useState('');
const submit=async e=>{e.preventDefault();setError('');try{if(mode==='login')await login(form.email,form.password);else await register(form.name,form.email,form.password);go('/')}catch(x){setError(x.response?.data?.message||'Não foi possível concluir')}};return <div className="min-h-screen flex items-center justify-center p-4 bg-gradient-to-br from-pink-50 to-white"><form onSubmit={submit} className="w-full max-w-md bg-white rounded-2xl shadow-xl p-7 space-y-5"><div className="text-center"><div className="text-5xl">🍓</div><h1 className="text-2xl font-bold mt-2">Doces & Espetinhos</h1><p className="text-gray-500">{mode==='login'?'Entre na sua conta':'Crie sua conta'}</p></div>{mode==='register'&&<input required placeholder="Nome" className="w-full border rounded-xl p-3" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/>}<input required type="email" placeholder="Email" className="w-full border rounded-xl p-3" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><input required type="password" placeholder="Senha" className="w-full border rounded-xl p-3" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/>{error&&<div className="text-sm text-red-600">{error}</div>}<button className="w-full bg-pink-600 text-white rounded-xl p-3 font-semibold">{mode==='login'?'Entrar':'Criar conta'}</button><button type="button" onClick={()=>setMode(mode==='login'?'register':'login')} className="w-full text-pink-600">{mode==='login'?'Criar uma conta':'Já tenho uma conta'}</button></form></div>}