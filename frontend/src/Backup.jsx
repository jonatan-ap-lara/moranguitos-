import React from 'react'; import {api} from '../services/api';
export default function Backup(){const backup=async()=>{const r=await api.get('/backup/export');const blob=new Blob([JSON.stringify(r.data,null,2)],{type:'application/json'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`backup-doces-espetinhos-${new Date().toISOString().slice(0,10)}.json`;a.click();URL.revokeObjectURL(a.href)};return <div className="max-w-xl space-y-5"><h1 className="text-3xl font-bold">Backup</h1><div className="bg-white border rounded-2xl p-6"><p className="text-gray-600">Exporte seus dados para um arquivo JSON.</p><button onClick={backup} className="mt-4 bg-pink-600 text-white rounded-xl px-5 py-3 font-semibold">⬇️ Fazer backup</button></div></div>}