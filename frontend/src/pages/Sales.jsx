import React,{useEffect,useState} from 'react'; import {api} from '../services/api';
export default function Sales(){const [sales,setSales]=useState([]);useEffect(()=>{api.get('/sales').then(r=>setSales(r.data))},[]);return <div className="space-y-6"><h1 className="text-3xl font-bold">Vendas</h1><div className="bg-white border rounded-2xl divide-y">{sales.map(s=><div className="p-4 flex justify-between" key={s._id}><div><b>{s.items.map(i=>`${i.quantity}x ${i.productName}`).join(', ')}</b><div className="text-sm text-gray-500">{new Date(s.saleDate).toLocaleString('pt-BR')} • {s.paymentMethod}</div></div><strong>{s.totalAmount.toLocaleString('pt-BR',{style:'currency',currency:'BRL'})}</strong></div>)}{!sales.length&&<div className="p-8 text-center text-gray-500">Nenhuma venda registrada.</div>}</div></div>}